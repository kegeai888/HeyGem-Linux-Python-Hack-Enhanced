# HeyGem AI 数字人生成器 - 使用说明

## 🚀 快速启动

### 1. 使用启动脚本（推荐）
```bash
# 直接运行启动脚本
./start_app.sh
```

启动脚本会自动：
- ✅ 激活conda base环境
- ✅ 检查Python依赖包
- ✅ 检测并清理7860端口占用
- ✅ 清理内存和显存
- ✅ 检查GPU环境
- ✅ 安全启动应用程序

### 2. 手动启动
```bash
# 激活虚拟环境
conda activate base

# 检查并杀死占用7860端口的进程
lsof -ti:7860 | xargs -r kill -9

# 启动应用
python app.py
```

## 🎯 新界面特性

### 界面改进
- 🎨 **美观设计**: 现代化渐变色彩方案，专业视觉效果
- 📱 **响应式布局**: 适配不同屏幕尺寸
- 🔔 **实时反馈**: 文件上传状态实时显示
- 📊 **进度跟踪**: 详细的处理进度条和状态信息
- 🛡️ **错误处理**: 友好的错误提示和解决建议

### 文件处理增强
- 🌏 **中文文件名支持**: 完全支持中文、英文混合文件名
- 🔧 **智能文件名清理**: 自动处理特殊字符，确保系统兼容性
- 📁 **自动目录管理**:
  - 上传文件自动保存到 `inputs/` 目录
  - 音频文件: `inputs/audio/`
  - 视频文件: `inputs/video/`
  - 结果文件: `result/` 目录
- 📦 **文件完整性验证**: 自动检查文件大小和完整性
- 📝 **详细文件信息**: 显示文件大小、分辨率、时长等详细信息

### 预览功能
- 🎧 **音频预览**: 上传后立即试听，支持所有音频格式
- 📺 **视频预览**: 上传后立即观看，等比例缩放显示
- 🎯 **对比预览**: 原始文件与生成结果并排对比
- 📐 **智能缩放**: 自动调整显示尺寸，不影响界面布局
- ⚡ **即时加载**: 上传即可预览，无需等待处理

### 支持的文件格式

#### 音频格式 🎵
- **主流格式**: WAV, MP3, M4A, AAC
- **专业格式**: FLAC, OGG, WMA, AIFF, AU
- **文件大小**: 建议不超过100MB
- **时长建议**: 10秒-2分钟

#### 视频格式 🎬
- **常用格式**: MP4, AVI, MOV, MKV
- **其他格式**: WMV, FLV, WebM, M4V, 3GP, ASF
- **文件大小**: 建议不超过500MB
- **分辨率**: 建议720p以上
- **要求**: 必须包含清晰的人脸

### 功能增强
- 📊 **智能文件验证**: 自动检测文件格式和完整性
- 🧹 **内存管理**: 自动清理临时文件，防止内存泄漏
- ⚡ **性能优化**: GPU加速处理，队列管理
- 🕒 **处理进度**: 详细的处理步骤和实时进度显示

### 用户体验
- 📝 **详细说明**: 每个步骤都有清晰的指导
- 🎛️ **状态监控**: 实时显示处理状态和系统信息
- 🎨 **视觉反馈**: 丰富的图标和颜色指示
- 📱 **移动友好**: 在移动设备上也能正常使用

## 🌐 访问地址

启动成功后，可通过以下地址访问：
- **本地访问**: http://localhost:7860
- **网络访问**: http://[服务器IP]:7860

## 📁 文件管理

### 目录结构
```
HeyGem-Linux-Python-Hack/
├── inputs/           # 输入文件目录
│   ├── audio/       # 音频文件
│   └── video/       # 视频文件
├── result/          # 输出结果目录
├── temp/            # 临时文件目录
└── example/         # 示例文件
```

### 文件命名规则
- ✅ **支持中文**: 完全支持中文文件名
- ✅ **自动清理**: 特殊字符 `\/:*?"<>|` 会被替换为下划线
- ✅ **唯一性**: 自动添加时间戳避免文件名冲突
- ✅ **长度限制**: 自动处理过长的文件名

### 示例文件名处理
```
原始文件名: "我的视频 (测试版).mp4"
处理后: "我的视频_测试版_1758962002.mp4"

原始文件名: "Audio File!@#$%.wav"
处理后: "Audio_File_@.wav"

原始文件名: "特殊字符\/:*?"<>|.mp3"
处理后: "特殊字符_.mp3"
```

## 🔧 故障排除

### 常见问题

1. **端口被占用**
   ```bash
   ./start_app.sh  # 脚本会自动处理
   ```

2. **内存不足**
   ```bash
   # 清理系统缓存（需要root权限）
   sudo sync && sudo echo 3 > /proc/sys/vm/drop_caches
   ```

3. **GPU问题**
   ```bash
   # 检查CUDA环境
   python check_env/check_onnx_cuda.py
   ```

4. **依赖包缺失**
   ```bash
   conda activate base
   pip install -r requirements.txt
   ```

5. **文件上传失败**
   - 检查文件格式是否支持
   - 确认文件大小是否过大
   - 检查磁盘空间是否充足

6. **中文文件名问题**
   - 系统会自动处理中文文件名
   - 特殊字符会被安全地替换
   - 无需手动重命名文件

### 性能建议

- 🎮 **使用GPU**: 确保有可用的NVIDIA GPU和CUDA环境
- 💾 **充足内存**: 建议至少16GB系统内存
- 🚀 **SSD存储**: 使用SSD提高文件读写速度
- 🌐 **网络稳定**: 确保网络连接稳定
- 📁 **定期清理**: 定期清理 `temp/` 和 `result/` 目录

## 📊 系统要求

- **操作系统**: Linux (Ubuntu 18.04+)
- **Python版本**: 3.8（严格要求）
- **内存**: 16GB以上推荐
- **显卡**: NVIDIA GPU + CUDA 11.8+
- **存储**: 10GB以上可用空间
- **文件系统**: 支持UTF-8编码（推荐ext4或XFS）

## 🔍 测试功能

运行文件处理功能测试：
```bash
# 测试文件处理功能
python test_file_handler.py
```

测试包括：
- ✅ 中文文件名处理
- ✅ 特殊字符清理
- ✅ 文件格式验证
- ✅ 目录自动创建
- ✅ 文件复制和重命名

## 🆘 技术支持

如遇问题，请联系：
- 📧 **技术支持**: 科哥微信 312088415
- 🏢 **项目版本**: 仙宫云特别版
- 📚 **项目文档**: 查看README.md获取更多信息
- 🐛 **问题反馈**: 优先检查文件格式和系统要求